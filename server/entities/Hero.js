// * typescript seems cool but it's more of a nuisance for me as of right now

function Hero()
{
    // * contains the attributes of a hero, acts as documentation I guess
    this.name = "";
    this.type = "";
    // todo the rest of what makes a hero (deep)
}